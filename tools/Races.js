const { inherits } = require("util")
let species_stats = {};

species_stats.humans = {
  sante: {
    min: Number,
    max: Number,
    value: Number,
  },
  vigueur: {
    min: Number,
    max: Number,
    value: Number,
  },
  mobilite: {
    min: Number,
    max: Number,
    value: Number,
  },
  faille: {
    min: Number,
    max: Number,
    value: Number,
  },
  fragilite: {
    min: Number,
    max: Number,
    value: Number,
  },
  rusticite: {
    min: Number,
    max: Number,
    value: Number,
  },
  mental: {
    min: Number,
    max: Number,
    value: Number,
  }
}